<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.somya.appdevproject">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".userClothActive"></activity>
        <activity android:name=".furnitureActive_Activity" />
        <activity android:name=".providerActive_Activity" />
        <activity android:name=".provideroActivity" />
        <activity android:name=".educationActivity" />
        <activity android:name=".gadgetActivity" />
        <activity android:name=".furnitureActivity" />
        <activity android:name=".clothingActivity" />
        <activity android:name=".createTenderActivity" />
        <activity android:name=".forgotPassword_Activity" />
        <activity android:name=".requesterActivity" />
        <activity android:name=".providerActivity" />
        <activity android:name=".Sign_In_Activity" />
        <activity android:name=".SignUp_Activity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>